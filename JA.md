# NTE-2D-Animation-Spine-Viewer

FModelでエクスポートされたJSONファイルから、NTEのアセット向けにSpineアニメーション（PNG連番・動画）を自動で抽出し、復元・一括書き出しするPythonスクリプトです。

Playwright（ヘッドレスChromium）を使用してブラウザをバックグラウンドで自動操作するため、手動でのブラウザ操作は一切不要です。

---

## 主な機能

1. **アセットの復元:**
   - `UISpine` フォルダ以下のFModel書き出し済みJSONから `.skel` および `.atlas` ファイルを復元します。
   - 対応するPNGテクスチャとまとめ、出力ディレクトリ内のアセット別フォルダに整理します。
2. **ヘッドレス環境での決定論的レンダリング:**
   - `spine-webgl` を用いて、各アセットのすべてのアニメーションを1フレームずつ正確にレンダリングします。
3. **デュアル動画出力（FFmpeg経由）:**
   - **通常再生用:** `<アニメ名>.mp4` （不透明、高画質、色やハイライトのぼやけを防ぐ `yuv444p` 形式）。
   - **背景透過用:** `<アニメ名>_alpha.mov` （ロスレスPNGコーデック ＋ アルファチャンネル対応）。
4. **自動クリーンアップ:**
   - 変換成功後、中間生成された連番PNGや抽出済みの生アセットを自動で削除します（FFmpegが失敗した場合のみ、フォールバックとして連番PNGが保持されます）。
5. **設定の永続化:**
   - 読み込み元および保存先のパスは、同じディレクトリ内の `nte_config.json` に自動保存されます。

---

## NTE固有の仕様への対応

NTEは、鳴潮（Wuthering Waves）とはFModelのエクスポート形式が以下のように異なります：
- `SpineAtlasAsset` と `SpineSkeletonDataAsset` が別々のJSONファイルに分かれている（例: `SP_YLYYZ1.json` + `SP_YLYYZ1_Data.json`）。
- アトラスのプロパティ名が小文字の `rawData` ではなく、大文字の `RawData` になっている。
- このスクリプトは、この2ファイル構成の構造に対応した抽出処理をネイティブで行います。

---

## 事前準備

コマンドプロンプトを開き、初回のみ以下のコマンドを実行してください：

```bash
pip install playwright numpy pillow
playwright install chromium
